# Scripts

Utility scripts for development, deployment, and maintenance.

## Examples

- `start_all.bat` – Launch API server and UI (Windows)
- `start_all.sh` – Launch API server and UI (Unix)
- `backup_mongo.sh` – MongoDB backup helper
- `deploy.sh` – Production deployment wrapper

Add scripts here as needed.
